const ROLES = {
  ADMIN: 'admin',
  USER: 'user',
};

const ORDER_STATUS = {
  PENDING: 'pending',
  FULFILLED: 'fulfilled',
  CANCELED: 'canceled',
};

module.exports = {
  ROLES,
  ORDER_STATUS,
};
